#include <bits/stdc++.h>

using namespace std;

#define lli long long int
#define FOR(i,a,b) for(int i=a; i<b; i++)

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    //TODO: Start your code here:

    int t; cin>>t;

    for(int i=0; i<t; i++){
        lli n;
        int m;
        cin>>n>>m;

        string s = to_string(n);
        int p = s.size();

        string q = to_string(m);

        string r = q * to_string(p);


        string l = stoi()


        // int mask = (1<<m);
        // lli j = (n+mask);
        // cout<<j;
    } 



    return 0;
}